{
  "title": "Configuration du Scraper E-commerce Multi-Plateformes",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "search_queries": {
      "title": "Liste des termes de recherche",
      "type": "array",
      "description": "Liste des termes de recherche pour les produits",
      "items": {
        "type": "string"
      },
      "example": ["laptop", "smartphone", "headphones"]
    },
    "platforms": {
      "title": "Plateformes à scraper",
      "type": "array",
      "description": "Plateformes e-commerce à scraper",
      "items": {
        "type": "string",
        "enum": ["amazon", "ebay", "walmart", "etsy", "shopify"]
      },
      "editor": "select"
    },
    "max_products_per_query": {
      "title": "Nombre max de produits par recherche",
      "type": "integer",
      "description": "Nombre maximum de produits à récupérer par terme de recherche",
      "minimum": 1,
      "maximum": 100
    },
    "enable_price_tracking": {
      "title": "Activer le suivi des prix",
      "type": "boolean",
      "description": "Active le suivi automatique des prix des produits"
    },
    "enable_trend_analysis": {
      "title": "Activer l'analyse des tendances",
      "type": "boolean",
      "description": "Active l'analyse des tendances de marché"
    },
    "output_format": {
      "title": "Format de sortie",
      "type": "string",
      "description": "Format des données de sortie",
      "enum": ["json", "csv"],
      "editor": "select"
    },
    "proxy_configuration": {
      "title": "Configuration proxy",
      "type": "object",
      "description": "Configuration des proxies pour le scraping",
      "properties": {
        "use_proxy": {
          "title": "Utiliser un proxy",
          "type": "boolean",
          "description": "Active l'utilisation de proxies"
        },
        "proxy_groups": {
          "title": "Groupes de proxies",
          "type": "array",
          "description": "Liste des groupes de proxies à utiliser",
          "items": {
            "type": "string"
          }
        }
      }
    }
  },
  "required": ["search_queries", "platforms"]
}